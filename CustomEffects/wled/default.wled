// default animation, used if segment.name not set

program default
{
   pixelCounter = 0

   function renderFrame() 
   {
     setPixelColor(pixelCounter, pixelCounter)
     pixelCounter = pixelCounter + 1
   }
  
}