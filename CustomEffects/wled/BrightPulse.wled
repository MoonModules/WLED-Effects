// fill all leds based on sampleAvg value
// idea by @tony

program BrightPulse
{

  function renderFrame() 
  {
    lum = constrain(sampleAvg * 256 / (256 - speed), 0, 255)
    fill(colorBlend(segcolor(1), segcolor(0), lum))
  }

}