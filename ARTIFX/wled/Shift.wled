// use a (precompiled) function (shift) to change lots of pixels
// idea by @Haribro

program Shift
{

  function renderFrame() 
  {
    setPixelColor(0, colorFromPalette(random()))
    shift(1)
  }

}